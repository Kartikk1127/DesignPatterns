This pattern is a creational design pattern that provides an interface for creating objects in a superclass, but allows subclasses to alter the type of objects that will be created. 

This pattern defines an interface for creating an object, but lets classes decide which class to instantiate. In other words, this pattern delegates the responsibility to instantiate a class to its subclasses.